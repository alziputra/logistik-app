/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  /* config network options here  */
  allowedDevOrigins: ['192.168.1.9']
};

export default nextConfig;
